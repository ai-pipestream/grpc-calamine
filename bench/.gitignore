# Build output (this crate is deliberately outside the workspace).
/target

# Machine-specific settings: addresses, workbook paths. Copy .env.example
# to .env and edit; the file never belongs in the repository.
/.env
